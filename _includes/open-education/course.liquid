<article class="open-edu-course row valign-top">
    <div class="half"><iframe title="vimeo-player" src="https://player.vimeo.com/video/{{ course.vimeo_id }}?title=0&byline=0&portrait=0" width="100%" height="300" frameborder="0" allowfullscreen></iframe></div>
    <div class="half">
        <h1 class="title-sm">{{course.title}}</h1>
        <div class="series">{{course.series}}</div>
        <p class="description">{{course.description}}</p>
        <p class="instructor">{{course.instructor}}</p>
        {% if course.logo %}
          <p>in partnership with </p>
          <p><img src="{{ 'images/logos/' | append: course.logo | asset_url }}" style="max-width:12rem"></p>
        {% endif %}
    </div>
    <div class="grow">
        <hr />
    </div>
</article>
